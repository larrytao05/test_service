package com.example.test_service.noise;


class noisetest {
    public noisetest() {

    }
    public static void main(String[] args) {
        /*ArrayList<Modifier> testModifiers = new ArrayList<Modifier>(Arrays.asList(new Modifier[]{new Climate(),new Difficulty(1,1,new int[]{10,10}), new WaterPresent(false)}));
        Mountain m = new Mountain(new int[]{10,10},2,testModifiers);
        for (int x = 0; x < m.getSize()[0];x++){
            for (int y = 0; y < m.getSize()[1]; y++){
                int[] out = m.getClimate(x, y);
                System.out.println(out[0]);
                System.out.println(m.get(x, y));
            }
        }

        //MountainRenderer render = new MountainRenderer(m,20);
        //HashMap<Double[], Double[]> mInfo = m.getMountain(20);
        //MountainRenderer mapRender = new MountainRenderer(mInfo, 20);


        //MountainImg img = new MountainImg(new int[]{10,10},40,2,testModifiers);
        //System.out.println("Seed = "+img.getSeed());
        */
    }
}
